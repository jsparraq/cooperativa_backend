const { UserExisting, EmailPassWrong, UserAuth, User, UserRole, UserNotAccepted } = require('./user.error');

module.exports = { UserExisting, EmailPassWrong, UserAuth, User, UserRole, UserNotAccepted };
