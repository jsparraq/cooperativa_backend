const User = require('./user.model');
const Loan = require('./loan.model');
const Account = require('./account.model');
const News = require('./news.model');

module.exports = { User, Loan, Account, News };
